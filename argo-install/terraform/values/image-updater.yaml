---
image:
  tag: "v0.12.2"

metrics:
  enabled: true

config:
  registries:
    - name: Docker Hub
      api_url: https://registry-1.docker.io
      ping: yes
      credentials: secret:argocd/docker-secret-updater#my-token
      limit: 20
      default: true